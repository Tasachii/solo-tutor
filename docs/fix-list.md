# รายการที่ต้องแก้ — Solo Tutor

> 5 ก.ย. 2569 · จากรีวิวโค้ด 2 รอบ (core 16 ข้อ · UI 41 ข้อ ทุกข้อพิสูจน์ด้วยการรัน) + เดินทุกหน้าด้วยมือ
> รวม 55 ข้อ จัดเป็น 4 ระดับ · **P0 ต้องเสร็จก่อนให้ครูจริงแตะ · P1 ก่อนขึ้นจอกรรมการ**

## แก่นของปัญหา

เกือบทุกข้อ P0 มีสาเหตุเดียว: **UI ถูกเขียนตอนที่มีแต่โหมดเดโม แล้วไม่ได้ไล่กลับมาเช็ค `state.mode === 'real'` ตอนเพิ่มโหมดจริง** — ผู้ปกครองเห็นพร้อมเพย์ปลอม ใบเสร็จจริงมีป้าย "ข้อมูลสมมติ" ปุ่มกลับเดโมลบข้อมูลจริงด้วยข้อความยืนยันผิด · แก้ P0 ทั้งชุด = ไล่ทุกจุดที่แตะ demo ด้วย flag เดียว

---

## P0 · ก่อนให้ครูจริงแตะ (17 ข้อ) — เงินหรือข้อมูลเสียหายเงียบ ๆ

| # | ปัญหา | ผู้ใช้เจออะไร | ที่ |
|---|---|---|---|
| 1 | **ผู้ปกครองเห็นพร้อมเพย์ปลอม** `08x-xxx-xxxx` + QR ป้าย "ตัวอย่าง" | กดลิงก์จากบิล LINE แล้ว**จ่ายไม่ได้** วงจรเงินตายตรงนี้ | `ClientPreview.tsx:83` ใช้ `PROMPTPAY_DISPLAY` แทน `state.provider.promptpayId` |
| 2 | PromptPay ปลอมของเดโมหลุดเข้าโหมดจริง ถ้าครูแก้แค่ชื่อ | ใบเสร็จจริงพิมพ์บัญชีปลอม | `scenarios.ts:174` เช็คแค่ชื่อ ไม่เช็คเลขบัญชี |
| 3 | ใบเสร็จจริงพิมพ์ "เดโม · ข้อมูลสมมติ" · ปุ่มเขียนว่า "แนบสลิป (จำลอง)" · หน้าผู้ปกครองมีแบนเนอร์เดโม + ลิงก์ "กลับไปแอป" เข้าหลังบ้านครู | ครูยื่นใบเสร็จที่บอกว่าเป็นของปลอม | `Receipt.tsx:25,39,76` · `ClientPreview.tsx:22,49,105` · `SlipSheet.tsx:58` |
| 4 | **"กลับไปโหมดเดโม" ใช้ข้อความยืนยันของ "เริ่มใช้จริง"** | อ่านว่า "ล้างข้อมูลตัวอย่าง" กดตกลง → ข้อมูลจริงหายหมด ไม่มี undo | `AppShell.tsx:67` |
| 5 | ติดใน onboarding ออกไม่ได้ ทางออกเดียวคือ "ใช้ข้อมูลตัวอย่างแทน" ซึ่งกลับไปเดโมเงียบ ๆ | ครูที่ไม่มีลิสต์ Excel บนมือถือ กดปุ่มเดียวที่กดได้ → หลุดจากโหมดจริง | `AppShell.tsx:28` redirect · `Onboarding.tsx:103,106` |
| 6 | onboarding ไม่บังคับกรอกชื่อ | ใบเสร็จ "ผู้รับเงิน:  · " ว่าง | `Onboarding.tsx:36` |
| 7 | **กู้คืนไฟล์สำรองแล้ววันแช่แข็งที่วันในไฟล์** และสลับโหมดเงียบ ๆ ถ้าไฟล์เป็นคนละโหมด | กู้ไฟล์เมื่อสัปดาห์ก่อน → วันนี้เป็นสัปดาห์ก่อน เช็คชื่อลงวันผิดทุกคาบ · กู้ไฟล์เดโมทับข้อมูลจริง | `store.tsx:199` `restore` ไม่ทำเหมือน `hydrate` |
| 8 | ไฟล์สำรองที่ขาดส่วน (มีแค่ `subjects`) ผ่านการตรวจ → `messages.map` ของ undefined → **จอขาว** | กู้ไฟล์ที่แก้มือ/ตัดท้าย แอปตาย | `backup.ts:32` ตรวจ 1 ใน 11 collection · `migrate()` ก็เหมือนกัน |
| 9 | storage เสีย → แทนด้วยเดโมเงียบ ๆ แล้ว**เขียนทับของเดิมใน 300ms** | ข้อมูลจริงหายถาวร เห็นแค่ toast 3 วิ | `store.tsx:250-274,302` |
| 10 | หน้าพัง (ErrorBoundary) ไม่มี CSS หัวข้อว่า "โหลดข้อมูลเดโมชุดใหม่" ปุ่มเดียวคือลบทุกอย่าง | ครูที่เจอบั๊กเสียข้อมูลทั้งเดือน | `ErrorBoundary.tsx` · `.crash` ไม่มีใน CSS |
| 11 | **งดคาบแล้วกดเอากลับมาไม่ได้** ปุ่ม "เอากลับมา" มีใน copy แต่ไม่มีใครเรียก และ completion ถูกลบถาวร | กดงดผิด → คาบหาย เงินหาย ไม่มีทางกลับ | `store.tsx:214-221` · `unitsOn` ซ่อน cancelled |
| 12 | เลื่อนคาบที่เช็คชื่อแล้ว `completedAt` ไม่ตาม `scheduledAt` → บิลเดือนเก่าลด แพ็กนับผิด เลื่อนแล้วต่อแพ็ก = คาบฟรีที่ไม่มีใครนับ | วันนี้ UI ซ่อนปุ่มเลื่อนไว้เมื่อเช็คชื่อแล้ว — แต่ reducer ต้องกันเอง ไม่ใช่พึ่ง JSX | `ledger.ts:32` · `store.tsx:202` |
| 13 | **สำรองข้อมูลบอกว่าสำเร็จเสมอ** แม้ iOS PWA ไม่ได้เซฟไฟล์ · และปิดคำเตือน 7 วัน | ครูเชื่อว่าสำรองแล้ว ไม่มีไฟล์จริง | `AppShell.tsx:81` · `backup.ts` คืน void |
| 14 | **คิวส่ง LINE เป็น state ในคอมโพเนนต์** → สลับไป LINE แล้ว iOS ทิ้งแท็บ → กลับมาเป็นร่างเหมือนเดิม → ส่งซ้ำ · "ยังไม่ได้ส่ง" ทิ้งทั้งคิวเงียบ ๆ · ไม่มีตัวบอกเหลือกี่คน | ผู้ปกครองได้บิลสองรอบ | `Admin.tsx:80-107` |
| 15 | `window.prompt` ใส่ยอดสลิปในโหมดจริง — พิมพ์ `1,200` ได้ NaN แล้ว**ไม่มีอะไรเกิดขึ้น** · iOS PWA อาจไม่เด้งเลย | รับเงินยอดไม่ตรงไม่ได้ | `SlipSheet.tsx:66` |
| 16 | ใบเสร็จของบิลที่จ่ายเป็นงวดบอก**ยอดงวดสุดท้าย** (2,000) ไม่ใช่ยอดบิล (3,000) · `receiptOfInvoice` หาไม่เจอเพราะเอา payment ตัวแรก | เอกสารการเงินผิด · CSV คอลัมน์ใบเสร็จว่าง | `store.tsx:98` · `receipts.ts:27` |
| 17 | วันไม่เดินถ้าเปิด PWA ค้างข้ามเที่ยงคืน (`todayISO` เรียกครั้งเดียวตอน mount) | เช้าวันใหม่ยังเห็นคาบเมื่อวาน เช็คชื่อลงวันผิด | `store.tsx:295` ไม่มี `visibilitychange` · `setToday` เป็นโค้ดตาย |

## P1 · ก่อนขึ้นจอกรรมการ (13 ข้อ) — เห็นบนเวที

| # | ปัญหา | ที่ |
|---|---|---|
| 18 | ปุ่ม/ชิป 4 ชนิดต่ำกว่า 44px: `.btn--sm` 38 · `.chip` 38 · `.sheet__x` 36 · `.toast__b` (undo!) 32 — และไม่โตตาม lg/xl | `index.css:97,113,191,205` |
| 19 | ป้ายเล็ก 11px (`.pk` `.badge` `.tagk` `.tab__badge`) ไม่ขยายตาม xl → บนจอฉายเล็กครึ่งหนึ่งของตัวอักษร | 9 จุดใน `index.css` |
| 20 | contrast ธีมสว่าง: `.warnbar` `.pk--low` 3.71:1 · `.stat--warn` 4.14:1 ตก AA (ธีมมืดผ่านหมด) | `--warn #a86a12` |
| 21 | ร่างชวนต่อค้างหลัง undo เช็คชื่อ → "วันนี้เรียนเป็นครั้งที่ **0** นอกแพ็ก" | `messages.ts:229` ไม่มี retract |
| 22 | โหมดจริงคนใหม่: แท็บแชทเป็นจอว่างเปล่า · Today empty state พาไปชีทที่ dropdown มีแต่ "—" · Billing หัวข้อ "รายเดือน" ลอยเหนือลิสต์ว่าง | `Admin.tsx:172` · `Today.tsx:87` · `Billing.tsx:86` |
| 23 | แดชบอร์ดเขียน "ช่วยไว้ X บาท · **ค่าบริการ 299 บาท**" — ราคาขายฝังในเครื่องมือของครูเอง อ่านเหมือนโดนเก็บเงิน | `Billing.tsx:76` |
| 24 | สถิติ "เวลาที่ใช้ ~2 นาที" อ่านว่าครูเสียเวลา ทั้งที่หมายถึงประหยัด | `copy.admin.timeSaved` |
| 25 | หน้าใบเสร็จเปิดมาที่เดือนปัจจุบันซึ่งว่าง ทั้งที่มีใบเสร็จเดือนก่อน | `ReceiptList.tsx:15` |
| 26 | "เริ่มใช้เดือนแรก ตัวเลขจะชัดสิ้นเดือน" โผล่ให้ผู้ปกครองที่เป็นลูกค้ามาสองเดือน | `ClientPreview.tsx:58` ยืม `firstMonthHint` |
| 27 | `window.confirm` 4 จุด — Android ขึ้น "localhost says…" · iOS PWA throttle · แปลไม่ได้ | `Today.tsx:142` `AppShell.tsx:67,74,88` |
| 28 | เลื่อนคาบรับวันว่าง (`scheduledAt: ''` → คาบหายถาวร) และวันในอดีต | `Today.tsx:135` ไม่มี guard/min |
| 29 | onboarding ราคาเริ่มต้น 400/3000/10×3500 แก้ไม่ได้ก่อนจบ — ครูที่คิด 500 ต้องไปแก้ทีละคน | `Onboarding.tsx:38` |
| 30 | **สี** — teal อมฟ้าเป็นค่าเริ่มต้น + ให้ผู้ใช้เลือกสีหลักเองในเมนู (คำขอใหม่) | token block + menu |

## P2 · UX ดีขึ้น (16 ข้อ)

| # | ปัญหา |
|---|---|
| 31 | "ลบออก" อยู่ติด "หยุดเรียน" ในแถวเดียวกัน 5 ปุ่มน้ำหนักเท่ากัน — ย้ายลบไปท้ายชีทแก้ไข |
| 32 | "งดคาบ" ซ่อนอยู่ในชีท "เลื่อน" — คนอยากงดต้องกดเลื่อนก่อน |
| 33 | ตัวเลขขวาสุดในแถวนักเรียนหมายถึงคนละอย่าง (ต่อครั้ง / ต่อเดือน / ราคาแพ็ก / ประมาณการ) ไม่มีป้าย |
| 34 | toast เก็บแค่ 2 → เช็คชื่อรัว 3 คน undo ของ 2 คนแรกหาย |
| 35 | ข้อความแจ้งเลื่อนไม่อัปเดตเมื่อเลื่อนซ้ำ (dedupe บล็อก + `rerender` ไม่รองรับ moved/cancelled) |
| 36 | popup โดนบล็อกแล้วบอก "คัดลอกให้แล้ว" ทั้งที่ `copyText` อาจล้มเหลว (ต้อง https) |
| 37 | ยกเลิก file picker ตอนกู้คืน → promise ค้าง เมนูค้าง |
| 38 | แก้ขนาด/ราคาแพ็กกลางทาง → ลูกค้าที่ซื้อไปแล้วกลายเป็น "เกิน 3" ทันที ไม่เตือน |
| 39 | `outstanding` นับเต็มบิลแม้จ่ายบางส่วนแล้ว — เงินก้อนเดียวอยู่สองฝั่ง |
| 40 | ชื่อไทยยาวตัดกลางคำใน `.srow__name` `.msg__hd` ใบเสร็จ (Today แก้แล้วที่เดียว) |
| 41 | "ดาวน์โหลด PDF" จริง ๆ คือ `window.print()` — iOS ต้องรู้เองว่าต้อง pinch |
| 42 | CSV ดาวน์โหลด 2 ไฟล์ห่าง 350ms → มือถือบล็อกไฟล์ที่สอง แต่ toast บอกสำเร็จ (โค้ดซ้ำ 2 ที่) |
| 43 | `theme-color` เป็นสีเก่า `#f6f7f8` ไม่ตรง `--bg` และไม่มีเวอร์ชันมืด |
| 44 | BottomSheet ไม่มี focus trap — Tab หลุดออกหลัง modal |
| 45 | `hydrated` เป็น setTimeout 300ms ปลอม ทุกแท็บโชว์ skeleton บนข้อมูลที่พร้อมแล้ว |
| 46 | ไม่มีตัวบอกว่าอยู่โหมดจริง (`copy.menu.realOn` มีแต่ไม่ได้ใช้) |

## P3 · เก็บกวาด (9 ข้อ)

| # | ปัญหา |
|---|---|
| 47 | string ไทยใน component 14 จุด · `SubjectDetail.tsx:43` หลุด `overdue`/`sent` อังกฤษ · "เกิน/เหลือ" ซ้ำ 3 ที่ |
| 48 | `aria-pressed` ขาด 2 ชุดชิป (`SubjectSheet.tsx:113` `WaitlistSheet.tsx:101`) |
| 49 | CSS ตาย (`.devbar*` `.picker*` `.land__why`) · copy keys ที่ไม่ได้ใช้ 10+ |
| 50 | `SubjectSheet` สร้าง id จาก `Date.now()` เปล่า ๆ — เพิ่ม 2 คนใน ms เดียวชนกัน |
| 51 | onboarding: step 3 ไปไม่ถึง · ไม่มีปุ่มย้อน · วางซ้ำได้นักเรียนซ้ำ · กด "เริ่มใช้งาน" รัวได้ |
| 52 | `invoiceToActOn` ใบ paid เดือนนี้ rank เท่า draft → เอาใบ paid มาโชว์ได้ |
| 53 | `--faint` ไม่ได้นิยามใน token (มี fallback) · `#fff` ใน print · fallback สีใน ShareCard |
| 54 | Landing ปุ่มบน "เดโม" ล่าง "ทดลองใช้" ไปที่เดียวกัน — เลือกคำเดียว |
| 55 | `/pitch` ไม่มีลิงก์จากที่ไหน — **ตั้งใจ** (ไม่เอา pitch mode) → ลบ route ทิ้งให้สะอาด |
| 56 | ทุกเทมเพลตถึงลูกค้าฝัง "ครับ" ตายตัว — ครูผู้หญิง (ผู้ใช้นำร่องทั้งสองคน) ถูกบังคับพูดเสียงผู้ชาย → `{p}`/`{pq}` + เลือกใน onboarding (บังคับเลือก ไม่มี default) ✅ |
| 57 | หลัง onboarding ไม่มีที่แก้ชื่อ/พร้อมเพย์ — พิมพ์เลขบัญชีผิดตัวเดียวต้องล้างข้อมูลทั้งหมด → เมนู "ชื่อและบัญชีรับเงิน" (โหมดใช้จริง) ✅ |
| 58 | `.omc/state/*` (telemetry ของ agent) ถูก commit ขึ้น repo สาธารณะ → untrack + ignore `.omc/` `.omx/` ✅ |
| 59 | เดโมล็อกวันที่ 2 ก.ย. 2568 — กรรมการดูปี 2569 จะเห็นปีเก่า · seed มี 60 วันที่ literal + เทส 80 จุดผูกกับมัน → **ไม่แก้ก่อนวันนำเสนอ** (เสี่ยงสูง ผลตอบแทนแค่ความรู้สึก) |
| 60 | Landing บรรทัด "เข้าไปลองได้เลย ไม่ต้องสมัคร…" → เอาออก · การ์ด "เวลาที่ใช้ ~1 นาที" ในแอดมิน → เอาออก ✅ |
| 61 | ไม่มีจุดให้เลือกว่าเก็บเงินแบบไหนก่อนเข้าใช้ → หน้า `/start` 4 แบบ + เมนู "รูปแบบการเก็บเงิน" · `state.style` ขับชุดข้อมูลเดโม/ค่าเริ่มต้น/ตัวกรอง · scenario ใหม่ `per-unit` `flat-heavy` ✅ |
| 62 | ตอนกด + เพิ่ม ไม่บอกว่าแต่ละวิธีนับยังไงหลังเช็คชื่อ → `modeHow` ใต้ตัวเลือก (ชีท + onboarding) ✅ |
| 63 | UI เรียบ ไม่มีสีสัน ไม่ friendly → โฉม Navy ทั้งแอป (token ก่อน component ตาม) · Landing ใหม่พูดกับฟรีแลนซ์ทุกอาชีพ · repo เป็น private (Pages ยังอยู่) ✅ |
| 64 | เมนูยาวเกินจอ (8 แถว + 5 กลุ่มชิป) → แยก 3 หมวด ทั่วไป · หน้าจอ · เดโม (โหมดจริงไม่มีหมวดเดโม) ✅ |
| 65 | ราคาตามแผนธุรกิจ rev.2: Free / Pro 299 / 3 เดือน 799 / 12 เดือน 2,490 · ตัด Concierge และรายไตรมาส · กล่อง "10 คนแรก เราตั้งระบบให้ฟรี" · FAQ ปิดเทอม = กดพัก · เพิ่ม FAQ ตรวจสลิป/ลบข้อมูล ✅ |
| 66 | **Roadmap ก่อน launch จริง (ยังไม่ทำ):** ฟีเจอร์ "พักชั่วคราว" (≤ 3 เดือน/ปี ไม่คิดเงิน ข้อมูลอยู่) · ปุ่ม "ลบบัญชีและข้อมูลทั้งหมด" + นโยบายเก็บข้อมูล (PDPA) · API ตรวจสลิปจริง (~0.3 บาท/ใบ) · อีเมลยืนยันสถานะกับ ธปท. — ทั้งหมดต้องมี backend/บัญชีผู้ใช้ก่อน |
| 67 | แผน rev.2 ข้อ 7 ขอให้ใช้ชื่อ "Solo" — **เจ้าของตัดสิน 6 ก.ย.: ใช้ Solo Tutor ต่อไป ไม่เปลี่ยนชื่อ** · repo กลับเป็น public ✅ |
| 68 | LINE OA วง 3 ข้อ 1: `supabase/migrations/0001_line.sql` + `src/core/lineDelivery.ts` (เลือกช่องทาง · โควตา 3 ชั้น · รหัสจับคู่ · ตรวจลายเซ็น) + เทส 12 ข้อ mutation ครบ 6 ตัว — **ยังไม่ต่อกับ UI และยังไม่ deploy** รอ Supabase ✅ |
| 69 | สำรองลง Google Sheets ของครูเอง — `src/core/sheets.ts` + `src/app/SheetsSheet.tsx` + `docs/google-sheets/` (Code.gs + คู่มือ) · เขียนอย่างเดียว ไม่อ่านกลับมาคำนวณ · เดโมไม่ส่ง ✅ |
| 70 | นำเข้า/ส่งออกรายชื่อในเมนู — อ่าน .xlsx เองด้วย DecompressionStream ไม่เพิ่ม dependency · CSV/TSV/วางจากชีต · เดาคอลัมน์แล้วให้แก้ · ราคารายคนตามไฟล์ · ส่งออก CSV ที่นำกลับเข้ามาได้ ✅ |
| 71 | มาสคอตเพนกวิน — ภาพหน้าแรก/หน้าว่าง · โลโก้ข้างชื่อแบรนด์ทุกหน้า · ไอคอน PWA และ favicon (เดิมยังเป็นสีเขียวชุดก่อนรีดีไซน์) · เพิ่ม icon-maskable สำหรับ Android · token `--beak` ✅ |
| 72 | LINE OA ข้อ 2–5: `src/core/lineProtocol.ts` (แปล webhook · แผนต่อเหตุการณ์ · ประกอบ request · แปลผล · retry · กรอบเวลาไทย · 1 ใบ/วัน) 18 เทส mutation 7 ตัว + Edge Functions 3 ตัวเป็นเปลือกบาง — **ยังไม่ deploy** รอ Supabase ✅ |

## เทสที่ยังขาด (จาก core review)

1. เลื่อนคาบที่เช็คชื่อแล้วข้ามเดือน / ข้าม `purchasedAt` ทุกทิศ
2. งด → เอากลับมา แล้วเงินต้องเท่าเดิม
3. dispatch `restore` แล้ว `today`/`mode` ต้องถูก
4. ไฟล์สำรองที่ format ถูกแต่ขาด collection ต้องถูกปฏิเสธ
5. จ่ายเป็นงวด → ใบเสร็จยอดรวม + `receiptOfInvoice` เจอ
6. ร่างที่เงื่อนไขหายไปต้องถูกถอน
7. ชื่อจริง + PromptPay เดโม → PromptPay ต้องว่าง
8. ข้ามเที่ยงคืน `today` ต้องเดิน

## ลำดับที่แนะนำ

**วันที่ 1** — P0 ข้อ 1–6, 10, 13 (ไล่ `mode === 'real'` ทั้งแอปในรอบเดียว) + ข้อ 7–9 (backup/restore/migrate ใช้ตัวตรวจร่วมกัน)
**วันที่ 2** — P0 ข้อ 11–12, 14–17 (ledger + คิว LINE + วันเดิน) + เทสที่ขาดทั้ง 8
**วันที่ 3** — P1 ทั้งหมด รวมสีใหม่ + เลือกสีเอง · ทดสอบบน iPhone จริง 1 เครื่อง
**ที่เหลือ** — P2/P3 ตามเวลา

ทุก commit: `npm run build && npm test && npm run e2e` · เทสใหม่ต้องพิสูจน์ว่าฟ้องแดงก่อนนับ
